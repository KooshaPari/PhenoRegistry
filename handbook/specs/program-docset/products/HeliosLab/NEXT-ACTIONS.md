# HeliosLab — next bounded outcomes

The existing owner continues valid work, not a new audit from zero. These are priority recommendations; actual permission, prerequisite and resource checks still apply.

## 1. Inventory all effective compiler profiles

**ID:** `CUR-1167587447-A1` · **Status:** proposed, not claimed or executed.

Map each supported package, app, test and generated segment to the intended type/lint/build job; document precise exclusions.

**Acceptance:** No supported code disappears without another checker or an accepted reason; known bad inputs fail.

## 2. Close scoped remaining errors and actual UX

**ID:** `CUR-1167587447-A2` · **Status:** proposed, not claimed or executed.

Reproduce the current diagnostics, fix real code defects without relaxing contracts, and exercise the accepted workbench/config workflow.

**Acceptance:** Clean selected build and a real persisted useful task; no percentage derived from excluded files.

## 3. Qualify cross-product configuration contracts

**ID:** `CUR-1167587447-A3` · **Status:** proposed, not claimed or executed.

Compare local custom configuration and session facilities with existing owned or mature components; test secrets and updates.

**Acceptance:** Dependencies resolve from a clean environment, secrets stay private, consumers retain semantics and the install works.

## Do not lose the rest of the contract

Use the existing dossier and pilot as context, not proof. Maintain the explanatory atlas, complete intent/spec contracts, independent QA matrix, qualified failure oracles, actual release/install evidence, and consumer-aware reuse. Close source-target-consumer dependencies with their owners. The next small repair must remain linked to the parent outcome: The accepted actual product builds, installs and performs its named user job with correct configuration/state handling.
