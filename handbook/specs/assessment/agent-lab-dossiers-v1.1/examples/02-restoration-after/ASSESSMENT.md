# Restoration / corrected behavior

Actual bundled fixture execution. Same two outcome checks; different implementation variants.

**EXECUTED_FIXTURE · EXAMPLE**

> Executed synthetic fixture, not a real-repository audit. Raw observations were produced by the included Python demonstration during this response. The two-predicate grader accepted a valid witness and rejected a no-op.

## Decision brief

**Next action:** Retain the verified fixture result; add independent failure modes before making any wider recovery claim.

**Stage:** Not a product stage. Two-predicate fixture passes.

This result supports only the two executed fixture predicates. It does not establish an MVP, a release, or production-safe restoration.

**Stop condition:** Stop any promotion that interprets these two toy passes as production backup safety.

## Identity and scope

- Dossier: `RESTORE-AFTER`; assessment: `ASSESS-after`; epoch: `E-TOY-1`.
- Beneficiary: The local fixture consumer reading the restored records, not an external user population.
- Boundary: Two-record JSON restoration fixture in a temporary local directory; not a production backup system.
- Subject digest: `sha256:1433edcb15661c3af159f06100a15bb8ba4b40ac35cf5a8d172ec44ac4237537`.
- Assignment byte digest: `sha256:afe771e15c13f04b151237ac7a382597531c31637661248cb9b292b09af0fe43`.
- Source revision: `NOT_AVAILABLE — no invented Git SHA`.
- Identity method: PEP value digest of the real bundled toy_product.py byte digest plus selected before/after variant in subject.json.
- Evidence cutoff: `2026-09-11T23:19:57.190324+00:00`.
- Registry: `NOT_REQUESTED`; signature: **UNSIGNED**.

Actual Python fixture execution only; no Git commit exists for this materialized package and none was invented.

## Scope, coverage, and gates

Two synthetic restoration obligations only. Not a product MVP or a real agent-run assessment.

**Admissible evidence reduction:** assessed pass rate 100.0%; assessment coverage 100.0%; verified satisfaction within settled applicability 100.0%.
**Current mandatory gate state:** `PASS`. Passing toy gates never implies real-product maturity.

| Gate | Scenario premise | Admissible evidence |
|---|---|---|
| TOY-RESTORATION | Not an illustrative case | PASS |

## Intent, genesis, and alternatives

Show that a successful-looking return message cannot substitute for restored data and corruption rejection.

**Authorship:** AGENT_DERIVED. **Intent status:** Accepted for local bundled demonstration only..

This fixture exists only to qualify the example grader. It is not a proposed new portfolio repository.

**Authority:** Only bundled synthetic code and temporary local files were executed. No network, credentials or user repositories were used.

**Alternatives:** Implement the capability in an existing owner; Use a suitable existing dependency; Run a narrower experiment; Do not build.

**Non-goals:** Validate a production storage system; Measure full product maturity; Claim an independent external auditor.

## Inventory and gap map

| ID | Capability / asset | Declared | Observation or premise | Gap |
|---|---|---|---|---|
| INV-01 | Restore operation | Claims restoration completed | Two required records restored with exact values | No gap in this two-record predicate |
| INV-02 | Corruption handling | Backup digest should match records | Corrupt backup rejected with ValueError | Only this corruption fixture was exercised |

## Filled rubric

Each row is scoped to this assignment. The full catalog is not claimed evaluated. `Display` is illustrative only where the banner says so. `Credit` is the underlying evidence-based reducer result.

| ID | Criterion / scoped title | Display | Credit | Mandatory | Weight |
|---|---|---|---|---|---|
| I-RESTORE | Restore exact record contents (`PEP-13-03-05`) | PASS | PASS | Yes | 1 |
| I-CORRUPT | Reject digest-mismatched backup (`PEP-13-03-03`) | PASS | PASS | Yes | 1 |

### I-RESTORE · Restore exact record contents

**Predicate:** Restored records preserve the required integrity predicates.

**Interpretation:** restored_integrity observed as True on the actual bundled toy variant.

**Reducer:** PASS — accepted input records with locally matching evidence digests.

**Next action:** Reuse only within this unchanged fixture/implementation/evaluator scope.

Evidence: [`EV-OBS`](raw/observations.json) · `sha256:dd88e71502430bead1624a16280513ddd8265ba53d3246dbeac38b7f63433fa6` · Actual execution of bundled synthetic example; no real product evaluation.

### I-CORRUPT · Reject digest-mismatched backup

**Predicate:** A corrupt backup is detected before claiming successful recovery.

**Interpretation:** corrupt_backup_rejected observed as True on the actual bundled toy variant.

**Reducer:** PASS — accepted input records with locally matching evidence digests.

**Next action:** Reuse only within this unchanged fixture/implementation/evaluator scope.

Evidence: [`EV-OBS`](raw/observations.json) · `sha256:dd88e71502430bead1624a16280513ddd8265ba53d3246dbeac38b7f63433fa6` · Actual execution of bundled synthetic example; no real product evaluation.

## Findings and bounded decisions

**DEC-NEXT / NO_ACTION / PROPOSED:** Retain the verified fixture result; add independent failure modes before making any wider recovery claim.

Alternatives: Implement the capability in an existing owner; Use a suitable existing dependency; Run a narrower experiment; Do not build.

Reversal: Stop any promotion that interprets these two toy passes as production backup safety.

## Maturity and value

| Axis | Scoped status | Basis |
|---|---|---|
| PRODUCT_VERIFIED | PASS — scoped fixture only | Exact restore and corruption predicates executed. |
| PUBLISHED_VERIFIED | NOT_EVALUATED | No publication was performed. |
| VALUE_VALIDATED | NOT_EVALUATED | No beneficiary study was performed. |

**Value evidence:** Educational demonstration of outcome-based verification; no real-product or market value measurement.

## Semantic review and contrary cases

### Does this assignment serve the parent outcome?

Judge the named consumer outcome rather than the existence of infrastructure or the size of the repository.

Support: The fictional scenario names a bounded consumer and a failure that would prevent its outcome.

Countercase: The same outcome may be served more cheaply by an existing owner, a smaller feature, or no new product.

Discriminator: Execute the smallest real consumer journey under the same success and failure conditions.

Confidence basis: Reasoned interpretation of the supplied fictional premises; no empirical product confidence is claimed.

## Delta and fresh-session handoff

**Change class:** PRODUCT_VARIANT_CHANGE. The selected implementation variant changes; the two acceptance predicates stay the same. The report records real fixture outputs, not a simulated repair by an autonomous agent.

**Accepted baseline:** Retain both implementation-variant digests, raw outputs, and the E-TOY-1 predicate set.

**Rejected or tested hypotheses:** Returning a success string establishes restoration.; A parseable but checksum-mismatched backup can be accepted..

**Next reproduction:** Run python core-v1.0/tools/demo_loop.py --out .local-runs/new-toy and inspect raw/observations.json in both snapshots.

**Budget and permissions:** No product mutation or external actions authorized by this package. Carry forward the actual cumulative budget and permissions from the operating mandate.

## Machine records and integrity

[Assignment](assignment.json) · [Assessment](assessment.json) · [Dossier context](dossier.json) · [Normalized JSON view](views/assessment.json) · [YAML view](views/assessment.yaml) · [JSONL export](views/records.jsonl) · [Build receipt](views/build.json)

JSON inputs are authoritative within this dossier. YAML, JSONL, Markdown, and HTML here are generated views. The unsigned manifest detects changed bytes against the supplied baseline; it does not certify observations or authorship.
