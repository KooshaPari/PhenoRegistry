# Input and build provenance

The retained core was extracted from `agent-lab-evaluation-system-v1.0-final.zip`.

Prior archive SHA-256: `54f0d4e5e9ed58387354aa10ca0e488dd5bd471ed5baaefcbbd949389b856f2e`.

The core's own file manifest passes unchanged. The dossier addition contains new authored context, example premises, report tools, and tests. `example-authoring-source.py` is an exact archival copy of the one-time build script; its absolute build path is historical, not a recommended runtime command. Use `tools/dossier.py` for normal rendering and verification, and the core demonstration tool for a new executed fixture run.

The six fictional case descriptions are deliberately inspectable and are not imported as executed evidence. No personal GitHub or device data was read during this addition. External format references are in `docs/SOURCES.md`.
