# Provenance and retained history

`history/v0.1/` and `history/v0.2/` preserve the exact files from the earlier conversation ZIPs. They are historical inputs, not multiple live authorities. The integrated v1 specification is the current handoff; implementation details and limits in the v1 guides take precedence over historical design-only descriptions.

The user requested an agent-operated lab in which local intent and repository creation may be agent-derived, and a comprehensive final ZIP. The current authorship added a new 1,080-entry candidate catalog, schemas, templates, procedures and tested reference kernel. Those additions were not recovered from local device/Git history. Historical repo observations in the source documents were not rerun against current repositories.

`history-inputs.json` records the original archive and retained file digests. `MANIFEST.json` at root hashes the final distributed bytes; neither provides authenticated identity. No portfolio repository was changed, no device audit executed and no live service integration performed by this packaging process.
