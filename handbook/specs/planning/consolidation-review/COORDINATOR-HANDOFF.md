# Consolidation lane: evidence repair and controlled continuation

This is a proposed operator handoff based on the supplied session export. It is not authorization to mutate repositories or override prior user decisions. Preserve valid product work; do not restart the portfolio audit or stop unrelated CVP development.

## Immediate assignment

Reconcile the consolidation coordinator's current state before issuing more bulk mutations. Pause the affected writers gracefully where possible, preserve their dirty worktrees/partial results, and do not kill sessions or delete working directories merely to get a clean slate. No new source-to-target movement, repository/branch deletion, force-push, admin merge, archive/rename or settings change is authorized by this handoff.

Use the actual available repo and workflow tools. AgilePlus and PhenoDocs remain tools, not portfolio registries. Reuse existing subject work records, portfolio decisions and evidence projections; do not invent a new canonical database.

## Required output: one outcome-gap table

For every source touched by the consolidation lane, include:

- Immutable source repository ID, observed names/aliases and source refs.
- Proposed/accepted target IDs, package paths, target refs and integration owner.
- Exact user approval or decision reference, its scope, later supersession/skip, and unresolved ambiguity.
- Issued operation ID, observed local/remote receipt, status and timestamp. Missing output is UNKNOWN, not success or failure.
- Claimed migrated capabilities versus what is actually registered, reachable, tested, packaged and consumed.
- Source preservation, releases/assets/metadata custody, unique branch work and restore evidence.
- Active worker generation, actual checkout, dirty state and overlapping target-file ownership.
- The next bounded accepted action, required owner, blocker and acceptance test.

Do not silently convert assistant summaries, TODO completion, repo descriptions or compaction memory into approvals. Do not treat the number of folders, bytes, files, branches, PRs or copied lines as semantic completeness.

## Specific reconciliation cases

### Agentora

Withdraw the unsupported brief that `src/` is a 6.5-million-line Python framework and that every one of 48 directory entries is a phenotype-prefixed Rust crate. Inspect actual manifests and sources. The observed package is Rust `agentkit` with `src/lib.rs` and `src/bin/main.rs`; mixed content elsewhere remains possible.

Map real packages, stubs, legacy duplicates, consumers and dependency closure. Do not continue a language-only split or move Rust material into a generic target solely because it is Rust. Reconcile the disputed `pheno` target and current paused status by immutable ID before planning further action.

### PhenoTooling absorptions

Preserve real landed work, including PRs #344 and #358. Verify the actual final target revision and all later dependents before proposing repairs or reversions. For DataKit, inspect Cargo metadata and the CI package-selection paths, not just explicit member text. For Journeys, account for the separately migrated/non-migrated presentation components before assessing donor retirement.

Administrator merge and conflict-free status are not final-head verification. Use the existing required check/review policy. Do not weaken gates or force pushes to make this batch appear complete.

### PolicyStack and ResilienceKit

The export contains reported JSON/error-output losses and a mismatch between an eleven-line rate-limit error crate and a later full-capability claim. Locate actual current implementations and compare behavioral contracts. Do not assume those past gaps still exist; do not assume syntax parsing or cargo check already resolved them.

### Skips and later exceptions

Reconstruct the timeline for pheno, landing, nanovms, DevOps, PlayCua/Eidolon and the late split dispatches. Specific later MCQ answers may change particular decisions; they do not establish a universal destination policy. Later skips must be propagated to descendants, including after compaction/restart.

The September 15 quartet—PhenoProject, RichCliKit, PhenoRuntime and SubstrateAdaptersBundle—did receive named deletion approval. Preserve that fact. Independently assess the accuracy of the safety brief and receipts; do not falsely call this quartet unapproved or generalize its exception to other sources. Do not re-delete, restore or recreate any source automatically.

## Correct the small instruments first

Replace `.archived // "DELETED"` as an existence verdict. False is a valid archived field value. Unknown access, redirects, absent metadata and HTTP 404 must remain distinct from a confirmed deleted immutable object.

Bind API schema to endpoint type: a missing REST `isFork` field does not establish non-fork status. Inventory fork-network membership sufficiently for the operator policy. Track bytes and LOC separately. Enumerate packages from manifests and metadata rather than counting directory entries.

Require negative controls for these observations. Use inert fixtures or safe read-only checks; do not test deletion detection by deleting a real repository.

## Resume one proven transition

Use separate source and target worktrees per worker. Grant exclusive ownership for shared manifests, lockfiles, migrations and release configuration. Reconcile completed side effects before retrying; fence the old worker generation so a lost network stream does not create competing mutations.

One transition must prove source capability disposition, target dependency/build inclusion, meaningful positive and negative tests, consumer compatibility, packaging/install where applicable, and historical preservation. Retain the accepted independent assurance floors; mark missing measurement as missing rather than fabricate coverage. Scoped evidence can be reused only when its revision/environment applicability is established.

Complete one migration and one actual usable consumer/CVP outcome before widening this lane again. Preparation complete, PR merged, target verified, consumer migrated and donor retired are separate statuses.

## Final report format

Return the outcome-gap table, exact source/target/approval references, independently verified facts, unknowns, and the next executable work package. Do not return another generic audit essay, an unqualified SAFE TO DELETE list, or a folder-presence checklist presented as finished migration.
