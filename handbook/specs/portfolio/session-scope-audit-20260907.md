# Portfolio session audit and recovery plan

Observation: 2026-09-06 Pacific / 2026-09-07 UTC. This is a generated audit artifact, not a new canonical portfolio ledger or authorization to migrate.

## Scope and failure

Human scope: all KooshaPari portfolio repositories, pilots and closure work grounded in Downloads prompt/docsets. Latest correction requires audit and planning before more implementation. Container direction remains OCI-oriented tooling, Podman + Apple Container, and the user-named WSLC on Windows (exact intended Windows backend remains unresolved; prior assistant substituted WSL2 without verification).

The assistant lost the active task, chose a recently modified unrelated contribution checkout, and treated repeated continuation messages as evidence that this inferred scope was correct. That inference was wrong. DeskcommCRM and token-monitor are external contribution candidates, not the owned portfolio pilot targets. Repeated baseline tests and packet row corrections did not advance the actual mandate.

## Governing source map

All paths below are relative to ~/Downloads unless stated otherwise.

| Source | Role and limits |
|---|---|
| phenotype-portfolio-control/ | Broad charter, completeness model, fair pilots, authority and preservation policy; 146-repo September 1 snapshot is historical |
| kooshapari_portfolio_rationalization/ | Per-repository hypotheses and closure proposals, not accepted dispositions |
| portfolio-reconciliation/ | Reconciles the two inputs; execution addendum and normalized queue; source prompts must not be concatenated as conflicting mandates |
| chat2-portfolio-audit-20260904/ | Existing doctrine inventory, implementation plan and evidence; refresh exact relevant refs before reuse |
| portfolio-slice-audit-2026-09-05/ | Later scoped audits and pilot waves; MAIN00 records partial coverage and explicit blocked operations |
| chat1-portfolio-p0-2026-09-05/ | Preservation/closeout evidence candidate; independent current-state validation pending |
| helios-forgecode-agentora-forensic-recovery/, 03-forensic-ssot-recovery.md, 04-polyrepo-ecosystem-consolidation.md | Supporting forensic/migration source sets; detailed conflict review pending |

Ran portfolio-reconciliation/scripts/validate.py: PASS, 146 imported rows, 20 open decisions, 22 work tasks, 12 JSON files, 2 input archives checked. Validator explicitly reports zero live repository audits. This is package integrity only.

## Recovered active pilot

Workspace: /Users/kooshapari/CodeProjects/Phenotype/repos/pheno-portfolio-demos/G1-Agentora/showcase-research-2026-09-05

Prior worker rpc_context recovered its assigned scope: Agentora Patch Review Workbench and capture supervisor. The frozen task is unicode-truncation-01. References: EXECUTION_DAG.md; docs/superpowers/specs/2026-09-07-capture-rpc-supervisor-design.md; docs/superpowers/plans/2026-09-07-capture-rpc-supervisor.md.

Worker's prior Task 1 review FAILED: object params enforcement, malformed frame coverage, unknown-method handling, illegal lifecycle transition coverage, and encoding size-limit coverage remain unresolved. contract_write reports no review fixes before interruption; protocol_fix is interrupted. Treat this as recovered review evidence, not freshly executed tests.

EXECUTION_DAG records: direct baseline verified only for its narrow repair; Agentora attempt budget exhausted with process output absent; pinned LangGraph dependency unavailable; voice eligibility/blind audition incomplete; capture isolation/target/focus/input routing unproven; expanded comparisons, media exports, final QA and operational study unexecuted. These are artifact-reported states; no runtime rerun in this audit. No desktop/XQuartz fallback is acceptable under the recovered capture contract.

## Detour effects and claim corrections

| Item | Evidence/state |
|---|---|
| registry-contribution-plan-20260904 | Live status: docs/indie-contribution-plan-20260904, ahead 2 of locally recorded origin/main; untracked pre-pr directory. Two commits predated this detour; do not attribute them to it |
| pre-pr/provider-apps.md and review.md | Detour created/updated these; historical assertions conflict, counts were corrected repeatedly; exclude all from owned-portfolio progress |
| /tmp/token-monitor-tm03.AOY2MP | External checkout and dependencies created; prior suite result is unrelated baseline evidence |
| /tmp/deskcommcrm.uX0FOW | External checkout and dependencies created; unit baseline was red; unrelated to portfolio acceptance |
| Podman | Live verification: Homebrew podman 6.1.1 installed, podman machine list empty. Interrupted install completed |
| Colima | Live verification: running QEMU aarch64, Docker runtime; started during detour with fallback authorization |
| Typecheck/lint claims | Earlier tools did not consistently retain terminal exit codes; disappearance of a process alone does not establish success |
| Docker cleanup claim | EXIT trap ran with errors suppressed; removal was not independently verified, so prior removal claim was too strong |

No cleanup, uninstall, VM stop, product mutation, push or migration performed during this audit. Existing artifacts preserved.

## Corrected work plan and gates

1. Recover scope: bind session to the portfolio program and the actual Agentora pilot; retain this incident trail. DONE for recovered scope; earliest full user transcript is not independently recovered.
2. Reconcile existing evidence before new builds: index later Downloads waves and active pilot artifacts by repo/ref/contract/owner; mark current, historical, conflicting and unknown. Retain all 146 imported rows, refresh live owned/affiliated/local-only census separately. Do not claim 146 current repos from a static snapshot.
3. Build one remaining-work ledger from portfolio-reconciliation/data/work-queue.json and existing pilot DAGs. Keep product-local authority; reference evidence instead of copying whole doc trees. Distinguish prerequisite data/contracts from scheduling priorities.
4. Resume Agentora only under a bounded assignment after audit: resolve five Task 1 review gaps and independent re-review. Then supervisor lifecycle, dispatch and evidence export according to the existing plan. Do not infer a renewed attempt budget or relax offline/capture constraints from generic proc.
5. Reconcile G1-agentapi, G2-Apisync, G3 and other portfolio families against their existing artifacts before dispatching new pilots. Each comparative pilot needs fixed inputs, same acceptance oracle, pinned target/alternatives, tuning budget, retained failures, and independent verification.
6. OCI migration lane: audit nanovms runtime adapters, consumers and existing Windows backend first; define capabilities for run/exec/ports/volumes/cleanup/build and platform limitations. Stage Podman and Apple Container acceptance on owned targets; carry Windows as unverified until tested on that platform. OCI image support does not imply Docker CLI/API equivalence. No new Docker-socket dependency as the migration target.
7. Closure lane: per repo connect intent -> capability -> implementation -> negative-control tests -> fair pilot -> package/release -> operational dogfood -> disposition. Source/test/doc counts cannot substitute for closure.
8. Migration execution gates: consumer map, accepted authority, immutable refs, preservation and tested restore/rollback before cutover. No bulk archive/reset/prune/force-push. Separate reviewable staging from external publication and destructive actions.

Dependency sequence:

Source/authority reconciliation -> live census + existing evidence ledger
  -> bounded family/role decisions -> comparative pilot contracts
  -> selected implementation + independent verification
  -> productization/dogfood -> approved migration or retained role

OCI work is a parallel capability lane; it does not make every independent portfolio audit wait for all runtimes.

## Honest coverage

This audit establishes the scope failure, identifies governing docsets, validates reconciliation-kit integrity, recovers the actual interrupted pilot, and checks local runtime setup. It is not a fresh deep audit of every repository or every historical session. Remaining census, later-wave crosswalk, complete side-effect inventory and current pilot source verification remain explicit work.

## Continuation: live owner census and pilot crosswalk

Authenticated GitHub owner refresh returned 139 repositories (30 private, 17 archived), versus 146 imported names. 111 names match, 28 returned names are absent from the import, and 35 imported names were not returned. These are name changes/visibility/transfer candidates, not confirmed additions or deletions. An initial public endpoint returned only 109; the authenticated result supersedes it for owner coverage. Host IDs retained in portfolio-live-refresh-20260907/authenticated-owner-repositories.json. Affiliations, collaborator and local-only scope remain incomplete.

Worker report: session-pilot-crosswalk-20260907.md. AgentAPI has reported health/version evidence but a failed message round-trip; Apisync has recorded tests but unresolved source/package version provenance; G3 has static/lineage evidence without provider execution or migration proof. These records do not close comparative pilots.

GitHub redirect-aware lookup resolved 27 of the 35 unmatched imported names to current `zz-*` or `zz-archive-*` names and retained their numeric/node IDs in `portfolio-live-refresh-20260907/historical-name-resolution.json`. Eight historical names returned HTTP 404: `homebrew-omniroute`, `omniroute-rust`, `rust`, `thegent-pr2-v2-uncommitted-2026-07-14`, `thegent-workspace`, `vibe-monitor`, `vibeproxy-monitoring`, and `vibeproxy-monitoring-unified`. A 404 is an unresolved identity/availability result, not deletion proof.

Next ready audit task: resolve those eight historical identities using preserved local clones, refs, and the broader affiliated-repository scope, while retaining REP aliases. In parallel, acquire per-run source/config/artifact manifests for the three pilot sets. Agentora implementation stays behind its recovered review and attempt/capture gates. REC-W-001 is PARTIAL, not complete. No source/remote mutation occurred.

Local-path inspection adds only limited evidence. `thegent-pr2-v2-uncommitted-2026-07-14` exists under two `phenotype-registry` worktree paths and resolves through that parent repository's git common-dir, so it is not independent local checkout evidence. `pheno/vibeproxy-monitoring-unified` likewise resolves to the parent `pheno` repository and has a deletion recorded in its worktree. These paths preserve historical/absorption candidates but do not resolve the eight GitHub 404 identities. The parent worktrees are dirty and were not modified.

Affiliated-scope observation: authenticated organization membership currently returns `zd-collective`, `Akoma-FE`, `SecondSightapp`, and `CSE-360-Group-9-Bookstore`, with 14 visible organization repositories. The authenticated collaborator query returns five repositories owned by `Dmouse92` (four) and `ahmedcod2` (one). These observations are retained separately in `portfolio-live-refresh-20260907/affiliated-repositories.json`; they are access evidence, not an assertion that every result belongs in the KooshaPari portfolio or resolves a historical alias.

Local-scope observation: a bounded scan of `CodeProjects/Phenotype/repos` found 99 distinct Git roots, 93 with an `origin` remote. Records are retained in `portfolio-live-refresh-20260907/local-git-roots.json`. This is a local preservation/worktree inventory, not a host-account census: nested roots, alternate remotes, and paths beyond the scan boundary remain separate reconciliation work.

The eight 404 names also produce no exact GitHub repository-search results under `KooshaPari`; this is retained in `portfolio-live-refresh-20260907/name-search-resolution.json`. One local preservation root, `zz-archive/2026-09-03-monorepo-consolidation/homebrew-omniroute-backup`, retains `git@github.com:KooshaPari/homebrew-omniroute.git` at `40c4bc9debecab379f22e1d5b4842e103bd3557c`. It proves a locally retained historical checkout, not remote availability or a successor identity. The other seven have no bounded local-root match. Their status is `UNKNOWN_REQUIRES_FORENSICS`.
