# Package inventory and precedence

## V3 original additions

- Formal design direction with 24 acceptance requirements; five architectural decisions.
- Scene grammar, visual-review rubric, renderer-routing guide and 20-purposeful-primitive vocabulary.
- 30-family machine/human capability matrix and nine-milestone dependency plan.
- 18 new task-specific agent skills, a six-beat storyboard and scene JSON/schema.
- Original Lumen Chamber interactive SVG/projection specimen, source modules and tests.
- Unexecuted native Blender source recipe and Remotion frame-adapter example.
- 24 annotated external references with inspection limits and 67-entry offline working catalog.
- Guarded additive PhenoDesign installer; no automatic remote changes or old-overlay execution.
- Current test results, screenshots, 96 authored film frames and the independently decoded MP4.

## Retained history

`prior-kits/phenodesign-creative-production-v2.zip` and
`prior-kits/phenoDesign-visual-production-v2.zip` remain byte-for-byte original archives.
The newest creative-production v2 is the default historical implementation reference;
the alternate v2 is selectively reconciled, never blindly stacked. The original frontend/
3D pack is retained within the historical packages. Their previous tests are not counted
as new V3 results. Their old instructions never override live repository ownership or
newer accepted implementation.

Use `MANIFEST.sha256` for file integrity. It is not a producer signature or an independent
proof of the rendered result. The source and tested consumer define what actually works;
`evidence/VALIDATION.md` records what does not yet have execution evidence.
