---
name: pd-scene-performance-renditions
description: "Budget scene quality across devices, motion preferences and resource pressure."
license: MIT
---

# Scene Performance Renditions

## Trigger and objective
Budget scene quality across devices, motion preferences and resource pressure.

## Inputs
Real target devices, key moments and operator resource constraints.

## Workflow
Measure transfer versus decoded memory, input/frame time, hidden/offscreen work and disposal. Use a semantic task-preserving rendition for constrained devices. Qualify GPU and fallback separately; render-worker concurrency is not agent-chat concurrency.

Read `docs/REVIEW-AND-ACCEPTANCE.md` from the canonical pack root. In a PhenoDesign checkout that
root is `creative-production/scene-experiences/`; in the standalone ZIP it is
the extracted V3 root. Read only the related docs/recipes, not every skill.

## Deliver
Measured budget, rendition decision, hardware identity and failure tests.

## Critical alternative / failure case
Do not report headless software results as physical GPU performance or remove the defining experience silently.

## Non-negotiable boundaries
Keep accepted design tokens, editable source and source rights. Work on owned
files/sessions and within approved tool/spend/network scope. Capture actual
outputs, report blocked backends honestly, and use the existing journey/evidence
authority rather than an agent-authored success flag. A cheaper or less animated
alternative wins when it preserves the same meaning and task with better results.
