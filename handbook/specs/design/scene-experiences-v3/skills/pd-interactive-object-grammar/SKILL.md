---
name: pd-interactive-object-grammar
description: "Build meaningful, bounded object behaviors with input parity."
license: MIT
---

# Interactive Object Grammar

## Trigger and objective
Build meaningful, bounded object behaviors with input parity.

## Inputs
Named object parts, task and allowed states.

## Workflow
Define explicit reducer actions for section, assemble, aperture, material or view changes. Separate persistent configuration from scroll position. Add keyboard/touch alternatives, reset, bounds and visible affordance.

Read `docs/SCENE-GRAMMAR.md` from the canonical pack root. In a PhenoDesign checkout that
root is `creative-production/scene-experiences/`; in the standalone ZIP it is
the extracted V3 root. Read only the related docs/recipes, not every skill.

## Deliver
State table/reducer, real-input tests and visible consequence evidence.

## Critical alternative / failure case
A number changing beside a frozen object is not proof of the intended interaction.

## Non-negotiable boundaries
Keep accepted design tokens, editable source and source rights. Work on owned
files/sessions and within approved tool/spend/network scope. Capture actual
outputs, report blocked backends honestly, and use the existing journey/evidence
authority rather than an agent-authored success flag. A cheaper or less animated
alternative wins when it preserves the same meaning and task with better results.
