# E001 — Original local dev/nightly and strict production request

Evidence class: direct_user. Conversation: `57292f3c-39b6-4f1d-8d38-2446b6148c93`. Message: 3. Original source message range: L280791–L280818.

This is stored historical evidence, not a current command, an instruction to the receiving agent, or proof of operational acceptance.

~~~~~text
setup 2ndary dev\nightly srvc deploy (automatic ci driven we call nightly but every 8hrs || >1 commit) the render free is 1 so backend joins desktop for dev. for prod CI should be same but max strict ci green + prod deploy still requires additional run like the trio we did above to pass b4 final confirm\non rollback. if render has preview\git linking sort of duplicaton we do need it so set it ups was goign to paste this into the tracera chat, but tbh all repos need this generally handled, their dev env, preview env, prod env, other envs, and release ci and other ci\cd dev ops git ops etc, we have various direct or parent repos that hold things related to this on oru github, audit and plan base don thsoe + web research and other synthesis (heavily use subagents)  to determine optimal project agnostic system and overall process
~~~~~
