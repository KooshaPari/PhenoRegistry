# E012 — Pilot verifier conclusion, not independent acceptance

Evidence class: assistant_assertion. Conversation: `f57e3c3f-fb9f-414d-a9dd-6ae2a573da67`. Message: 48. Original source message range: L681510–L681598.

This is stored historical evidence, not a current command, an instruction to the receiving agent, or proof of operational acceptance.

~~~~~text
I've verified the state of the Tracera pilot deployment system against the rollout plan. Here's what's actually complete versus what still needs attention.

## Verification Summary

**Complete and committed upstream (phenotype-tooling reusable workflows):**
- ✅ `nightly-dev-deploy.yml` (205 lines, `phenotype-tooling/.github/workflows/reusable/nightly-dev-deploy.yml`) — sound logic: commit-aware idempotency via Render API `commitId` (not a writable secret watermark), dev-strictness gates (init/build + focused tests), rollback-safe wait loop (fails on `failed`/`canceled`/`build_failed`), post-deploy health check.
- ✅ `prod-deploy-approval.yml`, plus 8 other reusable workflows (quality-gate, rust-ci, cargo-deny, etc.) all present.

**Implemented but UNCOMMITTED in the Tracera repo (working tree only):**
- `Tracera/render.yaml` — modified (dev + prod fallback services; dev `autoDeploy: false` driven by workflow, prod `oauth: github` OIDC + `autoDeploy: true`)
- `Tracera/.github/workflows/nightly-dev-deploy.yml` — calls the reusable workflow
- `Tracera/.github/workflows/branch-protection.json` — CI trio (`ci/lint`, `ci/test`, `CI`) + required review + linear history

The rollout plan at `plans/2026-09-11-nightly-dev-deploy-rollout-plan.md:6-12` marks Phase 1 as **COMPLETED**, which matches the file-level implementation. Phase 2 (Tier 1 repos) is marked NEXT.

## Gaps I found (things "completed" that aren't actually live/ready)

1. **Nothing is committed in Tracera.** All 3 deployment manifests are untracked (`git status --short` shows `??` on both workflow files, `M` on `render.yaml`). The pilot won't deploy until these are committed and pushed to `main` (with branch protection applied).

2. **Secrets not wired.** The caller only passes `RENDER_API_KEY`; the reusable workflow needs `RENDER_SERVICE_ID_DEV` (or the `dev-service-id` input) — see `nightly-dev-deploy.yml:88-97` pre-flight which hard-fails if it's unset. That secret isn't passed by the caller, so it must be set at repo level.

3. **Health-path mismatch.** Caller passes `health-path: /health`, but the dev service's `healthCheckPath` is `/healthz` (`render.yaml:21`). Whichever endpoint the server actually serves should match or the post-deploy health rollback gate (`nightly-dev-deploy.yml:182-205`) will report a false failure.

## Recommendation

The system is designed well, but "fully implemented" is only accurate at the file level — the Tracera pilot isn't deployed yet. To actually make it live I'd: commit `render.yaml` + the two workflow files (ideally via branch/PR to satisfy branch protection), confirm `RENDER_SERVICE_ID_DEV` and `RENDER_API_KEY` are set in the Tracera repo, and reconcile the `/health` vs `/healthz` health-path.

Want me to proceed with committing the Tracera pilot (create branch, add files, open PR) and aligning the health-path, or would you prefer I leave the working tree as-is and just hand off the gap list?
~~~~~
