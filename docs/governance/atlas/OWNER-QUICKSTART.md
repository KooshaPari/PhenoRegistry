# Open the one folder for your current repository

Use [the current index](portfolio/CURRENT-REPO-INDEX.md). Each row has exactly one owner chat and one complete packet under `products/<current-name>/`.

The packet contains START-HERE, STATE, NEXT-ACTIONS, AGENT-PROMPT, CURRENT-STATE.json, WORKER-RECEIPT.template.json, the preserved detailed DOSSIER and a planned PILOT. `.github` is intentionally a directory name matching the repository; show hidden files if your file browser hides it.

The latest policy is one owner chat, not the old paired/ten-seat schedule. Do not replace valid local work with this snapshot. Return actual state, reconcile the next actions and continue authorized work. A worker receipt is requested; none is fabricated in this package.
