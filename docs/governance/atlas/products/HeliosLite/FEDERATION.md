# HeliosLite — reciprocal federation assignment

**One existing owner remains. Proposed integration role: Headless provider + CLI host.** The repository ID from v1.2 is `1226104769`; refresh actual identity, accepted scope and local state before implementation. This is a contract extension, not proof of existing support or a command to build a second owner/chat.

## Responsibility

Contribute session/control/task capabilities and optional views while preserving foreign-session read-only rules.

## First useful proof

Open the same allowed session in two perspectives, interrupt/reconnect, and prove no duplicate tool side effects.

## Shared rules

Read [the federation contract](../../federation/README.md) and [the owner addendum](../../prompts/RECIPROCAL-FEDERATION-ADDENDUM.md). Preserve standalone usefulness, data ownership, permissions and source lineage. Opening an app changes perspective, not authority. Discover/resolve lazily; preauthorized safe integration can be automatic, but presence never grants access. Reuse external and owned facilities instead of copying implementations. Coordinate provider/consumer changes with their current owners and one integration lead.

Keep this work proportional to the real product. A CLI/provider, library, publication repo or game need not implement a generic desktop App Center. State the supported integration tier. Genuine product evidence, private journey capture, native interaction and independent accepted QA floors remain required. Do not interrupt already-ready delivery for a speculative whole-platform rewrite.

Record adopted existing mechanisms, supported host/provider profiles, required and optional capabilities, current/committed consumers, contribution slots, scoped grants, authoritative state, update/detach/recovery, resource budgets, proof and remaining blockers. No source movement, security bypass, publication or repo lifecycle authority is granted here.

## v1.4 capability and proof amendment

Read [PROOF-AND-GRADE.md](PROOF-AND-GRADE.md). Capability composition need not mount a peer UI; canonical design, multi-channel evidence and a scope-bound numerical grade are required. No native results or approvals are implied.
