# Khostty — next bounded outcomes

The existing owner continues valid work, not a new audit from zero. These are priority recommendations; actual permission, prerequisite and resource checks still apply.

## 1. Identify owned delta and distribution

**ID:** `CUR-1322607630-A1` · **Status:** proposed, not claimed or executed.

Return exact worktree/source base, local patches, intended role (standalone/embedded) and installed build.

**Acceptance:** Clear upstream/owned feature and compatibility map.

## 2. Run the terminal correctness corpus

**ID:** `CUR-1322607630-A2` · **Status:** proposed, not claimed or executed.

Exercise Unicode, input/IME, resize, alternate-screen, paste, shell integration and high-volume output using real native UI.

**Acceptance:** No regressions against upstream on mandatory terminal semantics; native captures and diagnostics.

## 3. Prove the actual differentiated use

**ID:** `CUR-1322607630-A3` · **Status:** proposed, not claimed or executed.

Qualify the embedding/integration or workflow the fork exists for, including lifecycle and updates.

**Acceptance:** A useful owned outcome with reproducible performance and safe update behavior.

## Do not lose the rest of the contract

Use the existing dossier and pilot as context, not proof. Maintain the explanatory atlas, complete intent/spec contracts, independent QA matrix, qualified failure oracles, actual release/install evidence, and consumer-aware reuse. Close source-target-consumer dependencies with their owners. The next small repair must remain linked to the parent outcome: The intended terminal or embedded terminal is usable and maintains the required semantics of its upstream.
