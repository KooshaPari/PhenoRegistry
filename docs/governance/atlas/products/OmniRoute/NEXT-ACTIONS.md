# OmniRoute — next bounded outcomes

The existing owner continues valid work, not a new audit from zero. These are priority recommendations; actual permission, prerequisite and resource checks still apply.

## 1. Verify handler dispatch parity

**ID:** `CUR-1226171396-A1` · **Status:** proposed, not claimed or executed.

Diff the extracted paths and run contract tests for all affected provider categories, auth modes, status codes, partial streams and cancellation.

**Acceptance:** Before/after behavior preserved on defined cases; no fallback converts errors into success.

## 2. Run a realistic routing stress/fault workload

**ID:** `CUR-1226171396-A2` · **Status:** proposed, not claimed or executed.

Exercise quotas, credential scope, retries, timeouts and backpressure under actual concurrency with bounded provider usage.

**Acceptance:** Correct failover/accounting and tail-latency/resource results, with negative cases retained.

## 3. Qualify actual deployed fork

**ID:** `CUR-1226171396-A3` · **Status:** proposed, not claimed or executed.

Identify the running source/build/config and update/rollback channel; reconcile upstream owned delta.

**Acceptance:** A real installation uses the tested handlers and honors consumer contract through deployment.

## Do not lose the rest of the contract

Use the existing dossier and pilot as context, not proof. Maintain the explanatory atlas, complete intent/spec contracts, independent QA matrix, qualified failure oracles, actual release/install evidence, and consumer-aware reuse. Close source-target-consumer dependencies with their owners. The next small repair must remain linked to the parent outcome: The owned router demonstrably handles the supported providers and failures through its actual deployed channel.
