> **v1.4:** apply [capability, design, bridge and numerical-grade clarification](CAPABILITY-PROOF-GRADING-ADDENDUM.md). Keep current owners; no extra permissions.

# Apply to existing owners — reciprocal application federation

This is an additive product/architecture requirement, not a restart or new permission grant. Keep one owner chat per current non-zz repository; use its FEDERATION.md and actual accepted scope. Reuse qualified existing external AND owned capability implementations before new code. Improve shared upstreams with real/committed consumers in mind; do not copy source/data to manufacture integration.

The required experience is that compatible installed apps discover and compose into one coherent workspace. Opening either app makes that app's perspective primary where that direction is implemented, while peer services/views are integral contributions. Underlying object, work, state and security authorities do not move. A composite can be an applet higher up. This is not an instruction to merge source repositories, delete apps, unify databases or make every product a plugin to a compulsory App Center.

Read federation/README.md, PRODUCT-CONTRACT.md and your owner role. Inspect existing app contracts and native facilities. Propose or implement a bounded host/provider path inside already-granted write authority. Record typed requirements/offers, recipe/version, scopes, data owner, contribution slots, service-instance policy, locality/resource budget, update/detach behavior and actual evidence. Distinguish linked launch, contextual handoff, embedded pixels and genuine semantic integration.

Automatic means eliminate repetitive setup inside approved scope—not bypass permissions. Use deterministic recipes and known interfaces. Learning may propose preferred layouts/providers/adapters, but may not execute invented glue, acquire secrets, copy credentials or grant itself consent. Bind actual principals/workspaces and keep a clear per-integration off switch.

Make integrated UI obey host navigation, keyboard/focus/accessibility, contextual commands, settings, search, errors and notifications without duplicate shell chrome. Keep useful standalone paths. Hide neither provenance nor failures. Use real native/provider behaviors, not fixture panels, fabricated receipts or simulated product screenshots.

Test both declared directions and installation orders, simultaneous hosts, incompatible versions, missing dependencies, revoked scope, provider crash, unknown outcomes, upgrade, detach/uninstall and data preservation. Keep independent 85%+ applicable suite floors and complete critical evidence. A valid manifest/plan is not behavioral or authorization proof. Protect the operator's foreground game/audio/workload.

Coordinate source/consumer changes with other owners and one integration lead. Do not delay unrelated CVPs for a universal composition engine. Start with one real pair, then a second consumer and a bounded nested composition. Attach failures, limitations, true integration tier and the exact next unblock action. No lifecycle completion is inferred from a schema, screenshot, open PR or copied folder.
