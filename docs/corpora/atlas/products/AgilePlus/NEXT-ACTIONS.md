# AgilePlus — next bounded outcomes

The existing owner continues valid work, not a new audit from zero. These are priority recommendations; actual permission, prerequisite and resource checks still apply.

## 1. Repair the real assurance verdict

**ID:** `CUR-1169608854-A1` · **Status:** proposed, not claimed or executed.

Use separate supported-profile unit/integration/E2E reports, approved denominators, real artifact retrieval and explicit missing/below-floor failure. Reuse existing trusted coverage parsers rather than handrolling weak counters.

**Acceptance:** Injected missing, wrong-SHA and below-floor reports fail. Critical obligations are complete; accepted independent floors are actually enforced.

## 2. Qualify current command and state behavior

**ID:** `CUR-1169608854-A2` · **Status:** proposed, not claimed or executed.

Exercise REPL comments, interrupted commands, two unrelated subject repositories, state persistence, MCP transitions and materialized views with exact engine receipts.

**Acceptance:** CLI/MCP operate on the intended Git root; restart and negative authorization cases pass without cross-subject state.

## 3. Deliver the actual selected release

**ID:** `CUR-1169608854-A3` · **Status:** proposed, not claimed or executed.

Build/install the promised CLI and desktop profile outside the checkout; capture update/rollback and a useful work lifecycle.

**Acceptance:** Distributed artifact identity, clean install and actual end-to-end evidence; no unbuilt product surface certified by a CLI-only build.

## Do not lose the rest of the contract

Use the existing dossier and pilot as context, not proof. Maintain the explanatory atlas, complete intent/spec contracts, independent QA matrix, qualified failure oracles, actual release/install evidence, and consumer-aware reuse. Close source-target-consumer dependencies with their owners. The next small repair must remain linked to the parent outcome: A cleanly installed tool manages a real subject through an accepted work lifecycle, with trustworthy independent evidence.
