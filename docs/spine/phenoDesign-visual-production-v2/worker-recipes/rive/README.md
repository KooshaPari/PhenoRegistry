# Rive on-device agent route

The reviewed official docs describe a CLI-driven authoring workflow and an agent guide. Begin with the installed CLI's help; have it scaffold its own project/AGENTS instructions and schema. Current reference commands include project creation, local docs/schema, verification, inspection and screenshots; obtain exact argument syntax from that installed version before execution.

Preserve authored RML/project inputs and generated runtime/editor outputs. Do not assume complete editor-to-CLI round-trip support: the reviewed overview labels sync-back functionality as upcoming. Test each actual workflow rather than extrapolating from the CLI feature list.

Native/CLI canary: author one named vector component, animate one property, define one real state/input, verify, export .riv, capture representative frames, then exercise that state in the actual consuming web/native runtime with keyboard/accessibility equivalence. Test reduced motion, interruption, invalid input and cleanup. Source verification and exported runtime behavior are separate checks.

No Rive executable, paid asset, .riv or claimed Rive test result is included; this is a qualification recipe for the receiving agent. See R01/R02 in resources/catalog.json.
